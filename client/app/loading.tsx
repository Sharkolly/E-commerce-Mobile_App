import React, { useEffect } from "react";
import { View } from "react-native";

const Loading = () => {
  return <View className="text-3xl font-bold text-red-500">Loading.....</View>;
};

export default Loading;
